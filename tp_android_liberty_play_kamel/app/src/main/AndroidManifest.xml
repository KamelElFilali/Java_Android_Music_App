<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.example.tp_android_liberty_play_kamel">
    <!-- permission pour fingerprint sensor -->
    <uses-permission android:name="android.permission.USE_BIOMETRIC" />
    <uses-permission android:name="android.permission.USE_FINGERPRINT" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".DetailsAlbumFragment"
            tools:ignore="Instantiatable"></activity>
        <activity android:name=".CardAlbumRecherche" />
        <activity android:name=".DialogSelectPlaylist" />
        <activity android:name=".ResultatRechercheFragment"
            tools:ignore="Instantiatable" />
        <activity android:name=".VideoBackground" />
        <activity android:name=".FragmentProfil"
            tools:ignore="Instantiatable" />
        <activity android:name=".FragmentPlayList"
            tools:ignore="Instantiatable" />
        <activity android:name=".FragmentRecherche"
            tools:ignore="Instantiatable" />
        <activity android:name=".FragmentBibliotheque"
            tools:ignore="Instantiatable" />
        <activity android:name=".BottomNavigationBar" />
        <activity android:name=".LoginFingerPrint" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>